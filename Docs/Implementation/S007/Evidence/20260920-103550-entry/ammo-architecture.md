# Proposed minimal integration — gated on baseline and animation audit
Preserve WeaponRuntimeState/WeaponController, existing fire resolver, input bridge and animation presentation. Replace weapon-local reserve authority with a configured ammo ItemDefinition and the owning PlayerInventory. Add only a bounded exact multi-stack removal operation if needed; use existing quantity query. Commit guard must be set before publishing inventory callbacks, and observers must see conserved committed state. Keep loaded rounds weapon-owned; never grant reserve in weapon startup.
Use existing reload timers and state, with a measured visual commit point and single authored timing source. Pause freezes; fire during reload rejected; death/disable/menu cancel, preserving already committed transfer. Event-driven UI reads magazine and inventory; bind/unbind per weapon/session. Existing tests using integer reserve fixtures need migration to actual inventory fixtures while retaining assertions.
Tentative initial baseline: 30 loaded, zero inventory reserve (matches S005 empty inventory; initial magazine is an explicit authored allowance). Retain current damage and loot tuning. No chamber, physical magazines, new weapon, persistence or economy rebalance.
Reviewed GDD archive, LS-DOC-07/08/10/23/24, P01, historical S007 sprint, combat behavior spec, S004 P5 report/risks, S005 report/architecture/spec/acceptance and S006 report/evidence architecture. Historical S007 describes streaming and is superseded by current user scope. LS-DOC-10 explicitly permits a simple round pool in first slice. Existing S006 LOOT_ARCHITECTURE.md, LOOT_PROFILE_SPEC.md and S006_ACCEPTANCE.md are absent; report/evidence and source are the available authority. S004 risks include stale resolved entries; no blanket closure inferred.
