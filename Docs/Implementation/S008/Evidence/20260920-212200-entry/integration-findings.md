# Integration findings
First PlayMode compile: new test used GetInstanceID, which Unity6000.5 marks an error. Replaced only the test identity API with GetEntityId. Original failed log retained.
Focused r2: 5/6 PASS. Two actual production-ray expeditions, UI transfer/reload/live Shambler damage, no-respawn, partial/dropped-item continuity, death/session cleanup and 10-expedition identity/conservation soak passed. One test incorrectly assumed return cancels reload. Inspection: PlayerCombatController.CancelGameplayActions(false) releases fire/aim but intentionally retains reload; SessionFlow pause freezes timer. Preserve S007 policy; revised test asserts no immediate precommit transfer, then exactly one normal commit, and no duplicate postcommit transfer. No combat production change.
