Removed weapon-owned ReserveAmmo setter, AddReserve, initialTotal and MaxReserve configuration. ReserveAmmo becomes a read-only inventory query. WeaponController no longer accepts or seeds integer reserve; startup reads authored startingMagazine=30 and existing empty session inventory. Tests explicitly populate inventory and retain original fire/reload assertions. Retired weapon maxReserve cap tests now verify inventory capacity is the only reserve capacity authority.
Inventory already had GetTotalQuantity; new TryRemove(ItemDefinition,int) is exact across stacks, single notification. At reload commit stage magazine and consumed guard before inventory removal publishes; validation failure rolls back staged magazine; event observers see both committed owners. No user callbacks occur between staging and inventory mutation. Reentrant or throwing observers cannot repeat transfer or leave ammo lost.
